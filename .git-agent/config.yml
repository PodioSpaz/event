scopes:
    - name: app
      description: application logic, models, services, and CLI commands in Sources/; does not cover testing or CI
    - name: fm
      description: Homebrew formula definition for package distribution in Formula/; does not cover release workflows
